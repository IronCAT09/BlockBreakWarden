# Gradle
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Mod (cross-version; per-version coordinates live in build.gradle.kts)
mod_version=1.0.2
maven_group=com.ironcat
archives_base_name=blockbreakwarden
